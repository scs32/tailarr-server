# Stack icons

Service logos shown on Magic Stack cards, bundled so offline installs
render them. Sourced from
[homarr-labs/dashboard-icons](https://github.com/homarr-labs/dashboard-icons);
each logo remains the property of its project. Filename = the catalog
service name (`StackRail.tsx` maps by name; services without a file get
a monogram tile).
